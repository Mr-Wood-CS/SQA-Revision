# Implementation

**If Statements**

In the program below, the user is asked to enter a password. 

If they enter “**hello123**”, access is granted.

!!! example "Password Checker"

	```Python linenums="1"

		password = input("Please enter the password")
		
		if password == “hello123”:
			print("Access granted")
	```

**If/Else Statements**

In the program below, the user is asked to enter a password. 

If they enter “**hello123**”, access is granted.

!!! example

	```Python
		age = int(input("Please enter your age"))
		
		if age >= 17:
			print("You ae old enough to drive.")
		else:
			print("Sorry, you are not old enough to drive.”)
	```

**If/Elif/Else Statements**

!!! example

	```Python linenums="1"
		
		mark = int(input("Please enter percentage mark"))

		if mark >= 70:
			grade = “A”
		elif mark >= 60:
			grade = “B”
		elif mark >= 50:
			grade = “C”
		else:
			grade = “F”

		print(grade)
	```

___

!!! warning "Key Points"

    1. The code that follows an If/Elif/Else statement is indented (tabbed in). This is only triggered if the condition is true.

    2. An If/Elif/Else statement always ends with a colon :

